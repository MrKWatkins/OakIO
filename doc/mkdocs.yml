site_name: MrKWatkins.OakIO
theme:
  name: material
  repo_url: https://github.com/MrKWatkins/OakIO
  favicon: favicon.ico
  features:
    - navigation.sections
  palette:
    - media: "(prefers-color-scheme)"
      primary: blue
      accent: blue
      toggle:
        icon: material/brightness-auto
        name: Switch to light mode

    - media: "(prefers-color-scheme: light)"
      primary: blue
      accent: blue
      scheme: default
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode

    - media: "(prefers-color-scheme: dark)"
      primary: blue
      accent: blue
      scheme: slate
      toggle:
        icon: material/brightness-4
        name: Switch to system preference
plugins:
  - search
  - macros
  - mkdocs-simple-hooks:
      hooks:
        on_nav: "hooks:on_nav"
extra_css:
  - stylesheets/overrides.css
  - assets/javascripts/converter.css
markdown_extensions:
  - attr_list
